
import DokkaComponent from "@graphglue/dokka-docusaurus"
import rawHTML from '!!raw-loader!./notify-current-road-speed-limit-.raw'

# notifyCurrentRoadSpeedLimit

<DokkaComponent dokkaHTML={rawHTML}/>
        